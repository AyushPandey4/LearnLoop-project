# LearnLoop Frontend
#
# No environment variables are required in production when using the vercel.json rewrite method.
# All API calls use relative paths ('/api/...'):
# - In local development: Vite proxy in vite.config.js forwards '/api' to http://localhost:5000
# - In production: vercel.json rewrites '/api/:path*' to your Render backend
